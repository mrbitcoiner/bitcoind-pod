services:
  bitcoind:
    container_name: bitcoind
    build: ./containers/bitcoind
    working_dir: /app
    volumes:
      - ./containers/bitcoind/volume:/app
    # environment:
    #   - name=value
    ports:
      - 8332:8332
      - 3000:3000
    # depends_on:
    #   - serviceName
    networks:
      - bitcoin

networks:
  bitcoin:
    name: bitcoin
    external: true
